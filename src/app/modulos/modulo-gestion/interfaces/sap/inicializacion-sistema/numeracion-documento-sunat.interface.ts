export interface INumeracionDocumentoSunat {
  u_BPP_NDTD        : string;
  u_BPP_NDSD        : string;
  u_BPP_NDCD        : string;

  // Serie de documento de entrega por defecto
  u_FIB_SDED        : string;
  // Serie de documento de traslado por defecto
  u_FIB_SDTD        : string;
}
